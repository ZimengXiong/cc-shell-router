## type directly into {zsh, fish} without launching CC

lazy asf? have your agent install:
```
setup gh/zimengxiong/cc-shell-router
```

otherwise, install
```sh
# codex
git clone https://github.com/zimengxiong/cc-shell-router.git ~/.cc-shell-router && ~/.cc-shell-router/codex-shell-router install codex

# claude
git clone https://github.com/zimengxiong/cc-shell-router.git ~/.cc-shell-router && ~/.cc-shell-router/codex-shell-router install claude

# pi
git clone https://github.com/zimengxiong/cc-shell-router.git ~/.cc-shell-router && ~/.cc-shell-router/codex-shell-router install pi
```
